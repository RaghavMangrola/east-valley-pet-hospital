Kitura East Valley Pet Hospital
========

Sample Kitura Project

## Usage

* run `swift build && .build/debug/hello-world` to start the server
* navigate to [http://localhost:8090/](http://localhost:8090/)